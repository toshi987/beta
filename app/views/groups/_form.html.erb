<%= form_with(model: @group, local: true, url: choose_new_or_edit_group) do |form| %>
  <% if @group.errors.any? %>
    <div id="error_explanation">
      <h2><%= @group.errors.count %> errors are existed。</h2>
      <ul>
        
      <% @group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="group_groupname">
    <%= form.label :groupname %>
    <%= form.text_field :groupname %>
  </div>
  <div class="group_groupname">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>
  <%= form.submit %>
<% end %>

<%= link_to "Return to index", groups_path %>